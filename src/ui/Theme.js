import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    common: {
      black: "#222831",
      white: "#EEEEEE",
    },
    primary: {
      main: "#393E46",
    },
    secondary: {
      main: "#FFD369",
    },
  },
});

export default theme;
